#include <bits/stdc++.h>
using namespace std;
int main() {
    long long size,helper;
    cin>>size;
    vector<long long>v;
    for(long long i = 0;i<size;i++){
        while(cin>>helper){
            v.push_back(helper);
        }
        for(long long j = 0;j<v.size();j++){
            
        }
    }
}