#include <bits/stdc++.h>
using namespace std;
int main() {
    string a;
    long long as = 0,bs = 0;
    vector<long long>A;
    vector<long long>B;
    while(cin>>a){
        A = 0;
        B = 0;
        for(long long i = 0;i<a.size();i++){
            if((a[i] =='A' and a[i-1] == 'A') or (a[i] == 'A' and i = 0)){
                A[as]
            }
        }
        cout<<max(A,B)<<endl;
    }
}