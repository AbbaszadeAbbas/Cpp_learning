#include <bits/stdc++.h>
using namespace std;
int main() {
  double a,b,c,d;
  cin>>a>>b>>c>>d;
  cout.precision(4);
  cout<<fixed<<(double)a+b<<endl;
  cout.precision(4);
  cout<<fixed<<(double)a+b+c<<endl;
  cout.precision(4);
  cout<<fixed<<(double)a+b+c+d;
}